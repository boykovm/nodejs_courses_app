<nav>
    <div class="nav-wrapper">
        <a href="/" class="brand-logo">Courses App</a>
        <ul id="nav-mobile" class="right hide-on-med-and-down">
            {{#if isHome}}
                <li class="active">
            {{else}}
                <li>
            {{/if}}
                    <a href="/">Main</a>
                </li>

            {{#if isCourses}}
                <li class="active">
            {{else}}
                <li>
            {{/if}}
                <a href="/courses">Courses</a>
                </li>

            {{#if isAuth}}
                {{#if isAdd}}
                    <li class="active">
                {{else}}
                    <li>
                {{/if}}
                    <a href="/add">Add Course</a>
                </li>

                {{#if isProfile}}
                <li class="active">
                {{else}}
                <li>
                {{/if}}
                <a href="/profile">Profile</a>
            </li>

                {{#if isCard}}
                    <li class="active">
                {{else}}
                    <li>
                {{/if}}
                    <a href="/card">Card</a>
                </li>

                {{#if isOrder}}
                    <li class="active">
                {{else}}
                    <li>
                {{/if}}
                    <a href="/orders">Orders</a>
                </li>
                <li>
                    <a href="/auth/logout">Logout</a>
                </li>
            {{else}}
                {{#if isLogin}}
                    <li class="active">
                {{else}}
                    <li>
                {{/if}}
                    <a href="/auth/login#login">Login</a></li>
            {{/if}}
        </ul>
    </div>
</nav>
